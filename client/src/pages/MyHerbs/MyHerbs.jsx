import { Link } from 'react-router-dom'
import { Header } from '../../components/Header.jsx'

export default function MyHerbs({ herbs, onOpenHerb, onScan }) {
  return <main className="app-shell collection-page"><Header active="My Herbs" onScan={onScan}/><section className="collection-heading"><div><p className="section-kicker">YOUR PERSONAL COLLECTION</p><h1>My Herbs</h1><p>Every plant you’ve identified, all in one place.</p></div><Link className="scan-button" to="/scan">📷 Scan an Herb</Link></section>{herbs.length ? <div className="collection-grid">{herbs.map((herb) => <button className="collection-card" key={`${herb.name}-${herb.time}`} onClick={() => onOpenHerb(herb)}><div className="collection-photo">{herb.preview ? <img src={herb.preview} alt=""/> : <span>✿</span>}</div><div><strong>{herb.name}</strong><em>{herb.scientific || herb.type}</em><small>{herb.score} match · {herb.time}</small></div><span className="collection-arrow">↗</span></button>)}</div> : <section className="empty-collection"><span>✿</span><h2>Your field notes start here</h2><p>Scan a plant to identify it and save it to your personal herb collection.</p><Link className="scan-button" to="/scan">Scan your first herb →</Link></section>}<footer className="dashboard-footer"><span>HERBLY · YOUR POCKET FIELD GUIDE</span><span>Explore with care <i className="status-dot"/></span></footer></main>
}
